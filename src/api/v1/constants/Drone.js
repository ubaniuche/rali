const LIGHTWEIGHT = 'lightweight'
const MIDDLEWEIGHT = 'middleweight'
const CRUISERWEIGHT = 'cruiserweight'
const HEAVYWEIGHT = 'heavyweight'
const IDLE = 'idle'
const LOADING = 'loading'
const LOADED = 'loaded'
const DELIVERING = 'delivering'
const DELIVERED = 'delivered'
const RETURNING = 'returning'

module.exports = {
    LIGHTWEIGHT,
    MIDDLEWEIGHT,
    CRUISERWEIGHT,
    HEAVYWEIGHT,
    IDLE,
    LOADING,
    LOADED,
    DELIVERING,
    DELIVERED,
    RETURNING,
    STATE: [
        IDLE,
        LOADING,
        LOADED,
        DELIVERING,
        DELIVERED,
        RETURNING,
    ]
}